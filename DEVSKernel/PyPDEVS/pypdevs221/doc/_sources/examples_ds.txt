Examples for Dynamic Structure DEVS
===================================

We used the same approach to Dynamic Structure as adevs, meaning that a *modelTransition* method will be called at every iteration on every model that transitioned.

TODO
